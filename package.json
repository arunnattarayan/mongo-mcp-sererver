{
  "name": "mongodb-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for MongoDB with read-only query capabilities",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "mongodb-mcp-server": "build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node build/index.js",
    "dev": "tsc && node build/index.js"
  },
  "keywords": [
    "mcp",
    "mongodb",
    "database",
    "model-context-protocol"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}